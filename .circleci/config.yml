version: 2
jobs:
  build:
    working_directory: ~/iugov_bot
    docker:
      - image: circleci/python:3.7.3
        environment:
          PIPENV_VENV_IN_PROJECT: true
    steps:
      - checkout
      - run:
          name: install dependencies
          command: |
            sudo pip install pipenv
            pipenv install --dev
      - run:
          name: run tests
          command: |
            mkdir test-results
            mkdir test-reports
            pipenv run flake8 --exclude=.venv* --statistics
            pipenv run pytest -v
      - store_test_results:
          path: test-results
      - store_artifacts:
          path: test-results
          destination: test-reports